# Seasar2 Dicon Sample

This fixture keeps the Seasar2 MVP scope narrow: dicon component discovery, naming-based service/dao role hints, property injection candidates, include references, and aspect/interceptor candidates.

CodeAtlas treats these relationships as static discovery facts with `POSSIBLE` confidence. They are not deterministic runtime impact paths in the MVP.
