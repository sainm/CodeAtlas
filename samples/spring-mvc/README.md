# Spring MVC Sample

This sample is a static-analysis fixture for the CodeAtlas MVP chain:

Controller -> Service -> Repository -> MyBatis Mapper -> SQL -> table.

It intentionally keeps only source and resource files. CodeAtlas parses it in
Spoon noClasspath mode, so the sample does not need to compile by itself.

