tasks.register('uiInfo') {
    group = 'help'
    description = 'Prints the placeholder UI module status.'
    doLast {
        println 'codeatlas-ui placeholder module; Vite/React scaffold will be added in the UI implementation slice.'
    }
}
